# 体育扫尾策略开发进度

本文档记录开发过程中的计划、状态、验证结果和遗留问题。

## 2026-04-27

### 已完成

- 新增业务需求文档：`docs/业务需求.md`。
- 新增目标设计文档：`docs/体育扫尾策略设计.md`。
- 新增二次开发计划：`docs/二次开发计划.md`。
- 新增验收与风控清单：`docs/策略验收与风控清单.md`。
- 新增直播状态输入设计：`docs/体育直播状态输入设计.md`。
- 将通用程序设计原则加入 `AGENTS.md`。
- 补充 `AGENTS.md` 的多 agent 协作规则：可并行探索、实现、文档、验证和评审，但必须明确职责边界、写入范围、整合责任和本地验证要求。
- 明确当前开发方式：计划驱动、进度记录、测试先行、按需多 agent 协作。
- 补充 `AGENTS.md` 测试原则：不得为了让测试通过而编写只覆盖当前测试的最小实现。
- 整合多 agent 探索结论，形成完整接线设计：`docs/体育扫尾完整接线设计.md`。
- 更新 `README.md` 和 `docs/README.md` 的文档入口。
- 完成策略包目标切片：
  - 新增 `src/strategies/current/sports_tail.py`，承载体育扫尾业务模型、执行权限、评估结果和拒绝原因。
  - 更新 `src/strategies/current/config.py`，默认配置转向体育扫尾，并提供完整 `SportsTailPolicy` 映射。
  - 更新 `src/strategies/current/outcomes.py`，从固定 `NO` token 改为 `Totals/Moneyline/Spreads` 方向解析。
  - 更新 `src/strategies/current/universe.py`，改为体育 market 级精筛。
  - 更新 `src/strategies/current/trading.py`，在 BUY 前接入体育扫尾统一门禁；缺直播状态、非自动权限或评估拒绝时不下单。
  - 更新 `src/strategies/current/recovery.py`，恢复逻辑按体育可管理 token 集合处理，不再依赖固定主 token。
- 新增目标切片测试：`tests/strategies/current/test_sports_tail_target_slice.py`。
- 同步更新策略 README、配置文档和市场发现链路示例。
- 修复盘口线解析风险：`4-5` 形式的 slug 盘口线不会误解析成事件日期。
- 接通 app 层候选审计元数据：
  - `EntryPlan` 新增 metadata，用于承载事件输入、候选原因和执行权限。
  - `EntryPlanner` 和 `TradingDecisionService` 支持传入 metadata，并将同一份事实传给 `size_entry()` 与 `decide_entry()`。
  - `TradingDecisionWorker` 将 orderbook 事件 payload 传入入场计划。
  - entry replay 支持 fixture metadata，并在序列化结果中输出 metadata。
  - 新增应用层接线测试，确认直播状态 metadata 能从 app 入口进入策略门禁并回到 `EntryPlan.metadata`。
- 接通 sizing 阶段体育评估：
  - 当前焦点候选在预算分配前执行体育扫尾门禁。
  - 缺直播状态、评估拒绝或非自动执行权限会让当前候选 `buy_budget_usdc` 归零。
  - sizing metadata 与 entry decision metadata 使用同一套候选原因和执行权限字段。
- 接通候选审计投影：
  - `TradingDecisionWorker` 在 SKIPPED 和 RISK_CHECK 事件中输出 `plan_metadata`。
  - 缺少直播状态导致的候选拒绝会发布 SKIPPED 审计事件，便于后续管理台复盘。
  - SKIPPED 候选审计事件使用 P3 优先级，避免反向占用 P0 交易队列。
  - 候选 metadata 主要由运行时候选投影提供；无套利或候选拒绝事件不进入数据库持久化 outbox。
  - Admin 审计事件响应新增 `payload` 字段。
  - 前端审计页支持选中审计或 outbox 事件后查看 JSON 载荷。
- 新增通用入场 metadata provider 扩展点：
  - `TradingDecisionWorker` 支持在构建入场计划前合并外部 provider 返回的运行时事实。
  - provider 只补 metadata，不解析体育状态、不判断下单。
  - 测试确认 provider 字段会进入 `plan_metadata`，可作为后续直播比分状态接入点。
- 补充 entry replay 测试，确认 fixture metadata 能驱动体育扫尾计划并输出回放 metadata。
- 接通真实直播状态 store/provider：
  - 新增 `EntryMetadataStore`，按 `condition_id`、`market_slug` 或 `event_slug` 保存入场前运行时 metadata。
  - `build_runtime()` 装配 `EntryMetadataStore`，并把它接入 `TradingDecisionWorker.entry_metadata_provider`。
  - provider 只读取内存状态并返回 metadata，不解析体育规则、不做下单判断。
- 接通候选展示与人工确认入口：
  - AdminService 新增直播状态写入/查询、体育扫尾候选投影和候选人工确认。
  - 新增 `/candidates`、`/candidates/live-states`、`/candidates/confirm` API。
  - `manual_confirm` 候选在人工确认后重新构建入场计划，并继续经过 `TradingService -> RiskManager -> OrderExecutor`。
  - 前端新增 `/candidates` 页面，展示候选、直播状态和人工确认结果；确认按钮只依赖服务端 `confirmable`。
- 补充人工确认测试，确认 Moneyline 候选不会自动下单，但带确认 metadata 后可生成 BUY intent 并经过风控执行。
- 接入外部直播状态源和字段归一：
  - 新增 `domain.sports_live` 内部 DTO，承接外部比赛、队伍、状态和同步快照。
  - 新增 `infra/sports/espn_client.py`，把 ESPN scoreboard payload 转成内部 DTO，并归一化超时、限流和响应错误。
  - 新增 `strategies/current/live_state.py`，把通用比赛状态匹配到当前体育扫尾策略 market，并通过策略 hook 生成 `sports_tail_game` metadata。
  - 新增 `SportsLiveStateWorker`，作为 P2 scheduler job 写入 `EntryMetadataStore`，不在 P0 路径请求外部 API。
  - 直播状态更新可发布 `ENTRY_SIGNAL_TRIGGERED`，TradingDecisionWorker 用既有入场链路重放判断，仍经 `TradingService -> RiskManager -> OrderExecutor`。
  - `/runtime`、`/workers`、`/metrics` 新增 `sports_live_sync` 快照；前端候选页展示同步状态。
- 补齐策略级风控：
  - 新增 `src/strategies/current/risk.py`，检查单场、单联赛、单日新增和连续亏损暂停。
  - 当日新增可从账户 `fills` 自动统计，外部清算口径仍可通过策略 metadata 覆盖。
  - `size_entry()` 和 `decide_entry()` 在 BUY intent 生成前执行同一套策略级风控。
- 补齐退出计划与异常恢复：
  - 新增 `src/strategies/current/exit_plan.py`，统一输出 `sports_exit_plan` metadata。
  - BUY 决策、follow-up SELL 和 recovery SELL 均写入目标退出价、恢复口径和异常状态策略。
  - ReconcileService 接入 market 级入场 metadata，恢复逻辑能读取直播状态；暂停、延期、取消、争议、退赛、结束或状态过期时暂停新增交易。
  - Reconcile action、diff 事件和 follow-up 事件保留策略 metadata，便于审计串联。
- 补齐管理台候选过滤与复盘字段：
  - `/candidates` 不再隐藏 `reject`，可用于复盘拒绝原因。
  - 新增按盘口类型、比赛状态、动作、执行权限、是否通过、是否可确认和联赛过滤。
  - 候选 payload 增加联赛、队伍、阶段、观测时间、风险原因和退出计划字段。
  - 前端候选页增加对应过滤条件和状态列。
- 补齐真实样本校验、阈值校准和成交复盘：
  - ESPN scoreboard 解析器新增离线 payload 解析入口，运行时 client 与样本校验复用同一套归一化逻辑。
  - 新增 `strategies.current.live_sample_validation`，可校验 ESPN 状态字段、market/game 匹配、命中别名和失败原因。
  - 新增 `tests/fixtures/sports_live/espn_nba_knicks_celtics_live.json`，覆盖 live 与 delayed/paused 状态字段。
  - 补充真实 ESPN/Gamma 成对样本：NBA `76ers vs. Celtics`、NHL `Bruins vs. Sabres`、MLB `Boston Red Sox vs. Toronto Blue Jays`，覆盖 scheduled 状态和 Moneyline/Spreads/Totals market 匹配。
  - 新增 `strategies.current.calibration`，支持 Moneyline / Spreads 批量样本、阈值组对比、盘口类型统计和带标签盈亏汇总。
  - `Position` 承接 Polymarket Data API 的 `realized_pnl/cash_pnl/current_value/cur_price/redeemable` 等字段。
  - 新增 `GET /trade-replays` 和前端 `/replay` 页面，按 `condition_id/token_id` 聚合 orders、fills、positions、audit payload、候选上下文和退出计划。
  - 新增 `polymarket_trader.app.trade_replay_validation`，用于把 `/trade-replays` 响应与 Polymarket Data API 或页面导出的 positions JSON 做离线对账。
- 优先处理真实运行卡顿中的数据库写入压力：
  - `DomainEvent -> LocalOutbox` 只承接订单状态、成交、持仓和必要账户交易快照。
  - 市场发现、盘口快照、候选拒绝和扫描状态不再进入数据库持久化 outbox。
  - `/runtime`、supervisor 和 metrics 使用轻量 WS 状态快照，避免把大订阅列表序列化成管理查询负载。
  - `/runtime` 市场样本字段显式命名为 `market_sample` / `registry.market_sample`，避免调用方误解为全量市场列表。
  - 市场订阅范围仍按 registry 全量市场计算；终态、结束时间已过或直播终态且无敞口的市场会在 reconcile 后移出 registry 和 WS 跟踪。
- Review 整改：
  - 交易盘口削峰移动到 `EventBus` 的 merge-key 合并，`TradingDecisionWorker` 不再用时间节流丢弃已出队盘口事件。
  - 市场发现扫描默认 SLA 恢复，前台性能问题不通过降低后台发现频率解决。
- 修复管理台候选查询 CPU 占满事件循环：
  - `/candidates` 读取 `EntryMetadataStore` 中已形成运行时事实的候选市场，或按显式 condition/token/slug 查询单个市场。
  - 前端展示不再触发全量 registry 同步构建入场计划；后台市场发现、订阅和交易判断仍按完整 registry 运行。
  - 前端候选列表使用服务端分页与 `has_more/source_markets` 展示读取范围，不复写策略判断。

### 验证记录

- 已撤回体育扫尾最小实现和对应测试，避免在完整目标边界未厘清前留下补丁式实现。
- 占位词扫描：通过。
- 局部实现残留检查：确认旧的最小实现测试文件已移除；当前 `sports_tail.py` 是按完整目标切片重新实现。
- `ruff check .`：通过。
- `pytest tests/strategies/current/test_sports_tail_target_slice.py -q`：7 passed。
- `pytest -q`：7 passed。
- `ruff check .`：通过。
- 旧非体育示例扫描：未发现残留。
- `pytest -q`：8 passed。
- `ruff check .`：通过。
- `pytest -q`：9 passed。
- `ruff check .`：通过。
- `pytest -q`：10 passed。
- `ruff check .`：通过。
- `pytest -q`：11 passed。
- `ruff check .`：通过。
- `npm --prefix frontend run typecheck`：通过。
- `npm --prefix frontend run lint`：通过。
- `pytest -q`：12 passed。
- `ruff check .`：通过。
- `npm --prefix frontend run typecheck`：通过。
- `npm --prefix frontend run lint`：通过。
- `npm --prefix frontend run build`：通过。
- `pytest -q`：13 passed。
- `ruff check .`：通过。
- `npm --prefix frontend run typecheck`：通过。
- `npm --prefix frontend run lint`：通过。
- `pytest tests/strategies/current/test_sports_tail_target_slice.py -q`：12 passed。
- `python3 -m compileall -q src/polymarket_trader/api/routes/candidates.py src/polymarket_trader/app/admin_service.py src/polymarket_trader/runtime/entry_metadata.py`：通过。
- `pytest tests/strategies/current/test_sports_tail_target_slice.py -q`：13 passed。
- `pytest -q`：16 passed。
- `ruff check .`：通过。
- `npm --prefix frontend run typecheck`：通过。
- `npm --prefix frontend run lint`：通过。
- `npm --prefix frontend run build`：通过。
- Playwright 打开 `http://127.0.0.1:5173/candidates`：页面可渲染；因后端 `127.0.0.1:8000` 未运行，API 请求出现连接拒绝错误。
- `pytest tests/workers/test_sports_live_state_worker.py -q`：2 passed。
- `pytest -q`：21 passed。
- `ruff check .`：通过。
- `npm --prefix frontend run typecheck`：通过。
- `npm --prefix frontend run lint`：通过。
- `npm --prefix frontend run build`：通过。
- `pytest -q tests/strategies/current/test_sports_tail_target_slice.py`：21 passed。
- `pytest -q`：28 passed。
- `ruff check .`：通过。
- `npm --prefix frontend run typecheck`：通过。
- `npm --prefix frontend run lint`：通过。
- `npm --prefix frontend run build`：通过。
- Playwright 打开 `http://127.0.0.1:5174/candidates`：候选页可渲染，新增筛选控件可见；因后端 API 未运行，页面 console 出现连接失败错误。
- `pytest -q tests/strategies/current/test_live_sample_validation.py tests/strategies/current/test_sports_tail_calibration.py tests/app/test_trade_replay.py`：3 passed。
- `ruff check .`：通过。
- `pytest -q`：31 passed。
- `npm --prefix frontend run typecheck`：通过。
- `npm --prefix frontend run lint`：通过。
- `npm --prefix frontend run build`：通过。
- `PYTHONPATH=src python3 -m strategies.current.live_sample_validation tests/fixtures/sports_live/espn_nba_knicks_celtics_live.json`：通过，报告 `passed=true`。
- Playwright 打开 `http://127.0.0.1:5175/replay`：页面可渲染，复盘导航、过滤、列表和详情区域可见；因后端 `127.0.0.1:8000` 未运行，页面 console 出现连接失败错误。
- `git diff --check`：通过。
- `pytest -q tests/strategies/current/test_live_sample_validation.py tests/app/test_trade_replay_validation.py`：6 passed。
- 所有 `tests/fixtures/sports_live/*.json` 通过 `strategies.current.live_sample_validation` 校验。
- 已按 `.env` 读取 `POLYMARKET_FUNDER_ADDRESS` 与 `DATABASE_URL`，自动拉起仓库内 PostgreSQL 开发库并初始化当前 schema。
- 使用 `.env` 中的 funder 地址访问 Polymarket Data API positions 成功；当前 Data API 返回 0 条 position，本地库也没有 market / order / fill / position / audit 样本，因此 `/trade-replays` 对账链路跑通但无真实成交样本可比对。
- `pytest -q tests/app/test_trade_replay_validation.py`：3 passed。
- `ruff check src/polymarket_trader/app/trade_replay_validation.py tests/app/test_trade_replay_validation.py`：通过。

### 持续观察

- 使用真实候选数据持续校验 ESPN market 匹配置信度、联赛覆盖和字段差异。

### 当前决策

- 后续实现必须同时考虑 `sports_tail` 纯业务模型、配置、universe、outcome 解析、trading 接入、recovery 和管理展示，不按单个测试最小闭合。
- 三类盘口必须共享候选和评估模型。
- 代码实现前可以先不写测试；测试用于验证目标实现，不倒逼局部最小实现。
- 当前不改 `ExtensionDecision`、`ExtensionHooks`、`TradingService`、`RiskManager` 和 `OrderExecutor`；`TradingDecisionService` 只做 metadata 透传，不加入体育策略判断。
- 候选展示走服务端候选投影；候选级拒绝原因不写入 `Market.reject_reason`，也不因无套利持续写数据库。
- 自动下单前必须能从 `ExtensionContext.metadata` 读取直播比赛状态；缺失时返回 `missing_live_game_state`，不会创建 BUY。
- 通用 app 层只负责透传和审计 metadata，不复制体育策略判断。
- 直播状态输入应通过通用 metadata provider/enricher 在入场前补全，框架只透传，不解析体育状态。
- 人工确认入口不是交易客户端直连入口，必须重新构建入场计划并通过 TradingService、RiskManager 和 OrderExecutor。

### 待验证

- 使用更多真实候选数据扩充 `tests/fixtures/sports_live/`，覆盖加时、取消/延期、MLB 缺剩余秒数和不同联赛队名别名。
- 用真实成交样本核对 `/trade-replays` 的 Data API PnL 与 Polymarket 页面/导出数据是否一致。

### 风险与关注点

- ESPN、NBA liveData、NHL score API、MLB Stats API、SofaScore 和 TheSportsDB 已通过聚合器接入；后续需要用真实赛事样本持续验证 market 文本匹配和源字段差异。
- Moneyline 和 Spreads 的确定性阈值需要通过回放和只记录模式校准。
- 候选确认已经接入受控入口，但真实执行仍依赖交易客户端、盘口热态和账户同步状态。

### 2026-04-28 多源直播状态

- 新增 NBA/NHL/MLB/SofaScore/TheSportsDB 直播状态适配器，统一输出 `SportsLiveGame`。
- 新增 `SportsLiveAggregateClient`，并行拉取 ESPN/NBA/NHL/MLB/SofaScore/TheSportsDB；单源失败不阻断健康源，同场比赛优先保留官方源状态，通用免费源状态冲突时只记录 `source_conflicts` 并由策略拒绝自动执行。
- `SPORTS_LIVE_STATE_SOURCE` 升级为 `SPORTS_LIVE_STATE_SOURCES`，运行态 `sports_live_sync.source_statuses` 展示每个源的成功、比赛数、错误和 `health`，可区分 `success_with_live_data`、`success_empty`、`cached`、`rate_limited`、`failed`。
- 已补充 SofaScore 免费公开 scheduled-events 源和 TheSportsDB 免费公开 eventsday 源；默认配置为 `espn,nba,nhl,mlb,sofascore,thesportsdb`，当前按 `SPORTS_LIVE_STATE_LEAGUES` 映射 NBA/NHL/NFL/MLB 等 sport path 后进入统一聚合。
- SofaScore 和 TheSportsDB 都加入本地限频缓存；冷启动限流返回带健康状态的空快照，已有快照时短期沿用缓存，避免后台 5 秒同步触发免费接口限流。
- TheSportsDB 经真实接口验证当前对 NHL/MLB 有效，已限制为 NHL/MLB 补充源；NFL 由 ESPN/SofaScore 提供比分、阶段和粗略剩余时间，但因为缺少控球、暂停、档位和距离，默认只进入人工确认。
- 多源聚合已补同场去重规则：官方源状态优先，免费源冲突不覆盖官方源；同队伍不同开赛日期保留为不同比赛，避免 MLB 系列赛被错误覆盖。
- MLB 已补 `baseball_state`，使用局数、上下半局、出局数、攻守方和占垒判断 Moneyline / Spreads / Totals，不使用伪造剩余秒数。
- 默认本地 universe 精筛已收口到 NBA/NHL/NFL/MLB 及对应通用球类 token；远端 discovery 仍可用 `sports` tag 扫描全量候选，但足球、网球、电竞不会仅凭体育大类进入自动交易候选。
- 自动交易主链路未改变；聚合器只提供 metadata，订单仍经过 `TradingDecisionWorker -> TradingDecisionService -> RiskManager -> OrderExecutor`。
