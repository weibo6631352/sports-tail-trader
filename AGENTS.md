# Codex Rules

本文件整理自仓库内的 `README.md`、`docs/设计文档.md`、`docs/市场发现链路.md` 以及各层 `README`，用于约束 Codex 在本仓库中的改动方式。

只保留跨模块、可长期复用的规则；策略阈值、关键词、仓位参数等易变细节仍以 `src/strategies/current/` 和对应策略文档为准。

## 1. 总体原则

- 策略差异进策略目录，通用能力进框架；不要为了单个策略需求先改交易主链路。
- 开发过程中如果发现更合理的套利策略业务优化点，应主动提出建议，说明当前问题、优化思路、影响范围和是否需要立即实施；除非属于当前任务必须闭合的主路径问题，否则先讨论方案与取舍，不擅自扩大改动范围。
- 如果某个字段、状态或配置只服务当前策略，优先留在策略包内，不进入 `domain`、`extension_api` 或全局 `Settings`。
- 不采用“补丁式开发”作为默认方案，不为了表面兼容堆叠旁路、特殊分支或一次性修补逻辑。
- 程序设计必须面向完整目标形态建模；即使当前只启用部分能力，也不得按单一场景写死流程、接口、状态或配置，避免后续补丁式扩展、旁路逻辑和重复链路。
- 优先选择最合适、清晰、高效且符合目标架构的实现方式；如果现有架构不适合，应先说明目标形态，而不是在不合适的结构上继续叠加复杂度。
- 发现架构边界不清、职责放错或需要新增模块时，可以先提出架构优化方案，必要时与架构师 agent 商议后再实现。
- 修改数据库字段或表结构时，以新的目标模型为准，不为旧表、旧字段或旧数据路径做兼容性包容；旧结构可以直接删除后使用新结构。

## 2. Agent 协作

- 对多模块、可并行或需要不同视角复核的开发任务，应主动考虑使用多个 agent 员工协作；架构探索、代码实现、文档同步、测试验证和代码评审可以按职责拆分并行推进。
- 并行协作前必须明确每个 agent 的职责边界、只读或可写范围、预期输出和冲突避免方式；涉及代码修改时，各 agent 的写入文件范围应尽量不重叠。
- 总协调 agent 负责拆分任务、整合结论、复核关键改动、维护计划和进度记录；不能直接把子 agent 输出当作最终事实，重要结论和代码结果需要本地验证。
- 不为并行而并行；紧耦合、位于关键路径或需要统一架构判断的工作，应由总协调 agent 统筹，必要时顺序推进。

## 2.1 实盘策略自主进化

- Agent 需要具备自主进化策略、自主挖掘问题、自主挖掘和分析市场的能力；实盘演练中应主动分析实时数据、发现策略瓶颈、提出并验证可落地的改进。
- 实盘策略进化不是一次性任务，而是持续闭环：跑实盘观察真实数据 -> 思考和定位瓶颈 -> 改进策略或链路 -> 验证测试 -> 重启/继续实盘观察 -> 再次思考和改进。除非用户明确要求停止、暂停或切换任务，agent 不应在单轮观察或单次改动后主动结束这类进化任务。
- 策略进化的目标是敏锐发现更多具备盈利可能的市场，并在已订阅市场中敏锐识别机会、快速完成成交；只要存在可解释、可验证、可风控的盈利可能，就应逐步纳入支持范围，而不是因为当前策略尚未覆盖就长期忽略。
- 策略进化范围包括但不限于扩展更多直播源 API 与市场匹配能力，支持更多体育市场和盘口类型，例如第一场、全场、第一节是否进球等；具体实现仍必须落在当前策略包或合适分层内，不能绕过交易主链路、风控和审计。
- 实盘演化的目标范围是整个体育市场；新增或发现的体育市场不得因为当前缺少专用直播源、盘口模型或执行规则而长期被静默忽略。代码应持续扩展直播源覆盖、市场匹配和盘口解析能力，让 Moneyline、Totals、Spreads、Yes/No prop、分节/分盘/局数/让分/特殊事件等盘口至少能被统一建模、纳入候选诊断并给出可审计拒绝或 record-only 原因；只有具备明确胜率判断、资金效率和风控闭环的盘口才能进入自动执行。
- 不得把 `single_game` 当作总体业务范围限制。`single_game` 只能表示“可使用单场直播比分源评估”的市场家族；系列赛、冠军归属、赛季归属、球员转会/下家、球员奖项和其他长期体育市场也属于目标范围，应该逐步补专用数据源、定价模型和风控模型。缺少专用模型时应以明确 family/type/reason 进入诊断或 record-only，不得错误套用单场比分源。
- 发现机会后应优先优化发现速度、盘口热态更新速度、决策延迟和成交链路延迟；每次优化都要保留可观测指标、拒绝原因和测试验证，避免为了速度牺牲风控、幂等和可审计性。
- Agent 可根据实盘候选、拒绝原因、盘口缺失、直播源匹配失败、成交失败和运行时指标主动选择下一轮演化方向；若改动会扩大交易风险或改变资金暴露模型，应先说明影响范围和验证方式。

## 3. 改动落点

- 改策略规则、筛选语义、定价、仓位和恢复策略，优先改 `src/strategies/current/`。
- 改通用契约、上下文对象和扩展接口，改 `src/polymarket_trader/extension_api/`。
- 改交易编排、恢复编排和管理操作编排，改 `src/polymarket_trader/app/`。
- 改外部 API、数据库、WS、outbox 适配，改 `src/polymarket_trader/infra/`。
- 改运行时状态、调度、队列和 supervisor，改 `src/polymarket_trader/runtime/` 或 `src/polymarket_trader/workers/`。
- 改人工查询和受控操作入口，改 `src/polymarket_trader/api/`。

## 4. 分层边界

依赖方向遵循：

```text
api / workers -> app -> domain
app -> infra / runtime / observability
infra -> domain
runtime -> domain
```

必须遵守：

- Domain 只放纯业务规则、领域模型和内部 DTO，不依赖 FastAPI、SQLAlchemy、Polymarket SDK、WebSocket client、环境变量或数据库查询。
- App 负责用例编排，不保存第二份业务真相，不硬编码某个策略的具体阈值语义。
- Infra 负责外部协议适配，外部 payload 必须先转成内部 DTO 再向上返回。
- Worker 负责搬运消息和调度，不直接实现业务规则，不直接调用 Polymarket SDK。
- API 只做参数校验、调用应用服务和响应序列化，不直接操作交易客户端或运行时热状态写锁。

## 5. 交易链路

交易订单主链路保持：

```text
event -> TradingDecisionWorker -> TradingDecisionService -> PortfolioAllocator -> RiskManager -> TradingService -> OrderExecutor -> outbox/audit
```

硬约束：

- `OrderExecutor` 是唯一允许创建、签名、提交、取消和替换订单的模块。
- `RiskManager` 是任何下单前的强制门禁；新增下单入口必须显式经过它。
- App 层只编排决策和执行流程，不维护绕过 allocator / risk / executor 的第二套交易事实。
- 业务扩展模块输出框架定义的决策对象，不直接调用交易客户端。
- Admin API 不是自动下单入口，也不是交易客户端直连入口。
- Persistence / outbox / 审计只能异步承接副作用，不能反向阻塞交易主链路。

## 6. 状态与并发

状态真相优先级：

1. Polymarket 实时事件与权威快照
2. 本地内存状态
3. PostgreSQL 审计与快照

执行规则：

- 交易主链路优先级最高，交易实时判断优先使用内存状态。
- 权威修正依赖 WS 和 REST。
- 数据库只用于审计、复盘、查询和恢复参考，不作为交易唯一真相来源。
- Admin 和 Persistence 读取快照，不直接持有热状态写锁。
- 状态访问按 `condition_id` 或 `token_id` 分片，不使用全局大锁保护全部热状态。
- 非关键锁等待超时后跳过并告警，不能无限等待。
- 阻塞 I/O 和 CPU 密集任务必须离开交易主事件循环。
- 数据库写入、日志、全量扫描、报表和 Admin 大查询不能反向阻塞 P0 交易路径。
- 交易主链路 worker 不等待后台维护或异步支撑 worker 释放资源。
- 后台任务只能异步承接副作用或做恢复参考，不能决定交易主链路是否继续执行。
- Reconciler 不在批量扫描任务中长时间持有交易状态写锁。
- 队列需要容量上限、可观测性和降级路径。

## 7. 结构改造

做结构性改造时，优先遵守以下原则：

- 优先一次性切到目标形态，不保留长期双路径。
- 不把关键接线留到后续补丁；主路径所需接线需要在同一轮改动内闭合。
- 不引入只服务过渡期的配置项、状态字段或旁路逻辑。
- 双路径适配层只在明确存在外部契约约束时保留，并且必须记录原因、边界和移除条件。
- 数据库 schema 调整不按外部兼容契约处理，不保留旧表旧字段的长期迁移路径或兼容读写双路径。
- 发现调用侧仍使用非目标命名时，优先修改调用侧，不要在框架层长期保留同义字段、别名、包装函数或重复枚举。
- 新增调度器、扫描器或后台任务时，要把运行时状态暴露为可观测快照，便于 supervisor / admin 查询。

## 8. 配置、命名与建模

- `.env` 和 `Settings` 只承载框架运行参数；策略参数不应变成框架必填环境变量。
- 策略参数写在策略包内，或通过策略自己的配置加载方式处理。
- 契约字段命名必须单义，避免一套名字对外、一套名字对内的长期双命名层。
- Domain 内金额和价格使用 `Decimal`，不要用浮点数表示交易金额。
- 策略常量集中管理，避免散落硬编码。
- 拒绝原因必须可审计，不能只返回 `False`。
- 跳过操作、降级处理和恢复动作必须保留可审计原因，不能静默忽略关键失败。

## 9. 注释与文档

- 框架和业务逻辑代码应使用标准中文补齐可读性注释，包括但不限于模块注释、类注释、函数注释、字段注释、关键业务步骤注释和边界条件说明，保证后续维护者能理解职责、输入输出、状态含义和处理意图。
- 新增接口、配置项、运行时状态或人工操作入口时，同步更新对应文档。

## 10. 测试原则

- 测试用于验证目标实现，不为了让中间验证阶段通过而引入临时补丁、兼容旁路或特殊分支；如果功能尚未闭合，可以在完整开发完成后再统一验证。
- 不得为了让测试通过而编写只覆盖当前测试的最小实现；如果完整目标边界、主路径接线或长期模型尚未厘清，宁可先暂停测试和实现，补齐设计后再按目标形态实现与验证，避免给后续留下补丁式扩展坑。
- 涉及自动下单、风控、资金/仓位计算、订单幂等、恢复和 reconcile 的行为改动，优先补覆盖核心失败场景的测试；纯查询、文档、日志和策略参数调整不强制补测试。

## 11. 常见误区

- 在 route 里写交易逻辑。
- 在 worker 里复制 discovery / 扩展业务判断。
- 在 framework 层加入当前业务扩展专属字段或阈值。
- 为了减少改动，在 Domain 或内部接口中保留长期同义命名。
- 为了让中间测试通过而增加临时补丁、兼容旁路或特殊分支。
- 让数据库、持久化、日志或报表路径决定交易热路径是否能继续运行。
